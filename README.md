# MMIO-INT-Driver
Memory-mapped I/O (MMIO) is a method for accessing hardware registers by mapping physical device addresses into the processor's virtual address space. The GPIO controller exposes several registers at fixed physical addresses
